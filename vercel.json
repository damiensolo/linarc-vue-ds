{
  "buildCommand": "pnpm build:app",
  "outputDirectory": "packages/nuxt-app/.output/public",
  "installCommand": "pnpm install",
  "framework": "nuxtjs",
  "devCommand": "pnpm dev",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

