{
  "buildCommand": "pnpm --filter design-system build && pnpm --filter nuxt-app build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nuxtjs",
  "outputDirectory": "packages/nuxt-app/.vercel/output"
}
